<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".StyloQZxCaptureActivity">
    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@+id/zxing_barcode_scanner"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:zxing_scanner_layout="@layout/layout_styloqzxscanner">
    </com.journeyapps.barcodescanner.DecoratedBarcodeView>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/shape_pageheader"
        >
        <!--
        <ImageButton style="@style/ToolbarButton" android:id="@+id/tbButtonBack"
            android:layout_gravity="center_vertical|right"
            app:srcCompat="@drawable/ic_arrow_bold_left"
            android:onClick="OnBack"/>
            -->
        <View style="@style/FakeView"/>
        <Button style="@style/PushButton"
            android:id="@+id/switch_flashlight"
            android:text="@string/turn_on_flashlight"
            android:onClick="OnSwitchFlashlight"/>
    </LinearLayout>
</LinearLayout>