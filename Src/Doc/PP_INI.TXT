; PP_INI.TXT
; Copyright (c) A.Sobolev, A.Fokin 1996-2003, 2004, 2005, 2006, 2007, 2008, 2009, 2010, 2011, 2012, 2013, 2014, 2015, 2016, 2017, 2018, 2019
; @codepage UTF-8
;
; Параметры файла настройки PP.INI
;
;
; ------------------------------------------------------------------------------
; Пути
;
[path]
;
; Путь к словарю данных
;
sys=h:\ppy\data\sys
;
; Путь к базе данных (по умолчанию)
;
dat=h:\ppy\data\base
;
; Путь к архивной копии базы данных
;
arc=h:\ppy\data\arc
;
; Путь к каталогу, в котором храняться некоторые, распространяемые
; с дистрибутивом файлы, необходимые для правильной работы различных функций.
;
pack=h:\ppy\pack
;
; Путь к каталогу временных файлов
;
temp=h:\ppy\data\tmp
;
; Путь к каталогу содержащему папки бизнес-показателей
;
BizScorePath=path

[server]
;
; Адрес компьютера, на котором запущен JobServer
; Это может быть как ip-адрес, так и символьное наименование.
; Например:
; server=servercomp
; server=192.168.0.15
;
name=server_address
;
; Номер порта JobServer'а
; Если этот параметр отсутствует либо его значение
; меньше или равно нулю, то полагается порт
; по умолчанию =28015
;
port=port_num
;
; Таймаут серверного сокета при чтении данных сервером от клиента в миллисекундах.
; по умолчанию = 60000
;
sockettimeout=milliseconds
;
; Таймаут клиентского сокета при чтении данных клиентом от сервера в миллисекундах.
; по умолчанию = 120000
;
clientsockettimeout=milliseconds
;
; Таймаут жизни серверной сессии после разрыва соединения с клиентом в миллисекундах.
; Допускается нулевое значение.
; по умолчанию = 60000.
;
ClosedSocketTimeout=milliseconds
;
; Таймаут ожидания серверной сессией возврата клиентской сессии после команды SUSPEND.
; Клиенская сессия может самостоятельно переопределить данное значение параметром команды SUSPEND (см документацию).
; Допускается нулевое значение.
; по умолчанию = 3600000
;
SuspendedSocketTimeout=milliseconds
;
; Таймаут ожидания серверной сессией хоть какой-то активности со стороны клиента.
; После завершения таймаута, если активности не было, сессия закрывается.
; по умолчанию бесконечность.
;
SleepSocketTimeout=milliseconds
;
; @v8.1.1
; Терминальная последовательность текстовых команд сервера.
; Возомжно одно из следующих значений:
; -1 - по умолчанию (без терминала)
;  0 - без терминала: команда считывается из сокета до тех пор, пока в сокете есть что-то.
;  1 - \xD\xA символ перевода каретки совмещенный с символом новой строки.
;      Стандартная для Windows последовательность перехода на новую строку.
;  2 - \xD\xA\xD\xA сдвоенная пара "перевод каретки/новая строка"
;  3 - \xD одиночный (8 бит) символ перевода каретки
;  4 - \xA одиночный (8 бит) символ новой строки
;  5 - \x0 одиночный (8 бит) бинарный ноль
; Клиентская сессия может переопределить терминальную последовательность командой SETTXTCMDTERM code
;
TextCommandTerminal=code

[drivemapping] ; Таблица сопоставления символов с сетевыми каталогами
L=UNC_PATH     ; Сисетма будет в путях интерпретировать букву как UNC-каталог.
               ; При этом подключение такого диска в операц системе не требуется.
; Пример: H=\\server\work
;
; ------------------------------------------------------------------------------
; Точки входа в базы данных
;
[dbname]
<name1>=Friendly Database Name 1,path1
<name2>=Friendly Database Name 2,path2
 .
 .
 .
;
; ------------------------------------------------------------------------------
; Конфигурация резервного копирования //
;
[backup]
UseCopyContinuous={1|0} ; Если значение параметра не 0, то при создании
						; копии на сервере базы данных происходит "горячее"
						; копирование, то есть пользователи при этом могут не
						; выходить из системы
script_name=dbname,arc_path,num_days,compress_flag[0|1],num_copies
;
; ------------------------------------------------------------------------------
; Системные параметры
;
[system]
;
; @obsolete since v3.0.11
; Если 1, то будут использоваться конкурентные
; транзакции, если 0, то обычные.
; По умолчанию конкурентные.
;
trans={1|0}
;
; @obsolete since v3.1.2
; Соотношение между количеством товаров в группе к общему количеству товаров (промилле)
; ниже которого расчет остатков ведется перебором товаров, а не перебором лотов.
; default = 10 (1%)
;
group_rest_calc_threshold=number
;
; Параметр может потребоваться для обратной совместимости
; с версиями более ранними, чем 3.9.0. Значением этого параметра является дата,
; такая, что документы имеющие дату ранее или равную этой, пересчитываютс
; с использованием старого метода расчета скидки. Новый метод правильнее (устраняет
; ряд проблем округления, однако ведет к пересчету на несколько копеек
; некоторых документов (особенно с дробными значениями количества).
; Суть изменения приводится в виде фрагмента пвсевдокода:
;   Старый метод
;       [суммарная скидка по строке] = round(pTI->Discount * qtty, 2);
;   Новый метод
;       [суммарная скидка по строке] =
;       [суммарная цена реализации по строке] - round((pTI->Price-pTI->Discount) * qtty, 2);
;
390DisCalcMethodLockDate=date
;
; Параметр для обратной совместимости с версиями
; более ранними, чем 3.9.18. Дата, указанная в качестве значения параметра
; ограничивает применимость метода к документам, имеющим равную ей или более
; раннюю дату.
; Суть изменения состоит в методе расчета общей скидки на документ с
; учетом налога с продаж (особенно, если по некоторым позициям документа
; установлена фиксированная цена или котировка.
;
3918TDisCalcMethodLockDate=date
;
; Параметр для обратной совместимости с версиями
; более ранними, чем 4.0.5.
; При новом методе общая скидка точнее распределяется по товарным строкам.
;
; Новый алгоритм состоит в следующем:
; 1. Рассчитывается общая сумма по всем строкам - A
; 2. Общая скидка на документ - D
; 3. Попутно находим строку, имеющую минимальное количество товара q[i] при
;    максимальной цене p[i]. Запоминаем ее индекс last_position.
; 4. Пробегаем все строки за исключением [last_position] и расчитываем скидку
;    на каждую из этих строк по индуктивной формуле
;    d[i] = round(p[i] * (D - s[i]) / (A - a[i]), 2)
;    где s[i] = sum{j:0..i-1}( d[j] )
;        a[i] = sum{j:0..i-1}( p[j] * q[j] )
; 5. Наконец, скидку по строке last_position рассчитываем по формуле
;    d[last_position] = round(s[i] / q[last_position], 2)
;
; Старый алгорит был следующим:
; 1. Рассчитывается общая сумма по всем строкам - A
; 2. Общая скидка на документ - D
; 3. Вычисляется скидка по каждой строке
;    d[i] = round(p[i] * D / A, 2)
;
405TDisCalcMethodLocDate=date
;
; Параметр для обратной совместимости с версиями,
; более ранними, чем 4.1.8.
; Новый метод исправляет проблему, возникающую при расчете скидки по
; методу версии 4.0.5. Проблема состоит в том, что на позиции с одинаковыми
; исходными ценами могла быть начислена различная скидка.
; В целом алгоритм остался тем же, но несколько усложнился.
;
418TDisCalcMethodLockDate=date
;
; Каталог, в котором следует сохранять файлы данных
; в случае их несоответствия отчету.
;
reportdatapath=v:\newreprt
;
; Список имен пользователей (через запятую),
; которые будут получать предупреждение о необходимости сделать резервную копию
;
backupuser=<user_name_list>
;
; Коэффициент запаса свободного дискового пространства
; при создании или восстановления резервной копии. Значение должно быть представлено
; в виде целого положительного числа, которое системой делится на 1000 для получени
; конечного коэффициента. Например: 1500 (коэффициент запаса 1.5)
;
bssfactor=<number>
;
; Временный каталог, в котором предварительно будет
; сжиматься резервная копия перед копированием в каталог-хранилище.
; Если этот каталог не указан, то система попытается использовать каталог
; прописанный в переменной окружения TMP, затем TEMP.
; В случае, если попытка определения временного каталога окончилась неудачей,
; будет сжимать данные непосредственно в каталоге-хранилице.
;
backuptemp=<path>
;
; Пароль для работы с фискальным регистратором
; Штрих-ФР. pw - пароль кассира, adm_name - имя старшего кассира, которое
; будет печататься в чеке Z-отчета.
;
ShtrihFRPassword=pw,adm_name
;
; Если этот параметр имеет значение 1, то некоторые функции начинают
; записывать отладочную информацию в файлы журналов
;
debug={1|0}
;
; Количество попыток чтения, задержка между попытками чтения (мс)
;
ComPortReadCycling=count,delay
;
; имя домена для проверки учетной записи при входе в papyrus
;
DomainName=domain_name
;
; использовать текущую учетную запись для входа в papyrus
;
UseSysAuth={1|0}
;
; В весах BIZERBA штрихкод передается полностью (5 знаков)
;
BizerbaWholeBarcode={1|0}
;
; Передавать на кассы только самые последние
; изменения в товарах при обновлении сессии, а не с начала дня //
;
OnlyLastUpdatedGoodsToCash={1|0}
;
; Если 1, то полная отмена блокировки кассовых узлов
;
CashNodeNoLocking={0|1}
;
;  Если 1, то функция PreprocessBrowser вызывается из ChangeFilt
;
PreprocessBrowserFromChangeFilt={0|1}
;
; ------------------------------------------------------------------------------
; Имя узла асинхронного кассового аппарата
;
[<async_cash_node_name>]
;
; Путь к файлам для импорта сессии (необязат)
;
path=in_path
;
; Наборы файлов для импорта сессий (1 или более наборов через ';'.
;
files=f1,f2,f3{;f21,f22,f23}*
;
; ------------------------------------------------------------------------------
; Параметры восстановления файлов
;
[recover]
;
; Каталог, в который записываются восстановленные файлы
;
path=recover_path
;
; Имя файла журнала восстановлени
;
log=log_file_name
;
; ------------------------------------------------------------------------------
; Параметры конфигурации
;
[config]
;
; @obsolete since v5.4.10 Символ, идентифицирующий тип
; регистрационного документа лицензии на право торговли отдельными видами товаров
tradelic=trade_lic_symb
;
; @obsolet since v4.0.6
; Печатать итог счета-фактуры на дне станицы
;
invoice_footer_on_bottom={1|0}
;
; Печатать счет-фактуру с колонками акциза
;
invoice_excise_columns={1|0}
;
; Имя файла с описанием этикеток принтера штрихкодов
;
label_file=file_name
;
; @v5.6.11
: Печать этикеток на принтере штрихкодов
; Задержка в миллисекундах между отправкой на принтер каждой
: строки товарного документа. Допустимые пределы [1..10000].
; Если параметр не задан или выходит за указанные пределы,
: то задержка принимается в 250 миллисекунд.
;
; Замечание: после каждой десятой строки программа формирует
: неуправляемую задержку в 2000 миллисекунд.
;
barlabelprint_delay=int_val
;
; Остатки товаров показываются с емкостью упаковки
;
goodsrest_pack={1|0}
;
; В диалоге строки товарного документа остатки показываются в упаковках
;
trfritem_pack={1|0}
;
; В фильтре товарных документов сразу устанавливать флаг "Показывать долги"
;
goodsbillfilt_showdebt={1|0}
;
; (Только DOS) Выбор товаров реализуется в экономичном (по памяти) режиме
;
ecogoodssel={1|0}
;
; Если база данных и имя пользователя указаны в командной строке, то
; система не выдает запрос на вход в систему. Пароль у пользователя должен отсутствовать.
;
nologinprompt={1|0}
;
; @v8.2.5
; Если параметр UseSjLoginEvent установлен в значение, отличное от 0, то при авторизации
; и при завершении сеанса создаются события в системном журнале LOGIN и LOGOUT.
; До версии 8.2.5 такие события создавались безусловно, однако теперь надобность в них
; практически отсутствует.
; По умолчанию - 0
;
UseSjLoginEvent={1|0}
;
; @v8.0.6
; Максимальный размер файла журнала, после превышения которого файл переименовывается и
; создается новый.
; Размер указывается в килобайтах (Kb).
; Например: MaxLogFileSize=65536 означает, что размер файлов не должен превышать 64MB
; По умолчанию: 32768 (32MB)
;
MaxLogFileSize=kb
;
; Сеанс пытается установить соединение с JobServer'ом.
; Адрес сервера берется из секции [server]
;
3tier={1|0}
;
; В сумму приходного кассового ордера включать налог с продаж из налоговых сумм
; Если по документу отгрузки налог с продаж не выделяется, а платеж должен включать налог с продаж,
; то в налоговой сумме, отражающей налог с продаж может быть указана сумма налога. Тогда сумма
; приходника увеличивается на сумму налога с продаж.
;
cashordincstax={1|0}
;
; @v4.3.3 Запрет на использование нового метода генерации
; штрихкода по шаблону. Аварийный параметр, на случай, если новый (более быстрый,
; но и более сложный) метод будет сбоить.
;
433OldGenBarcodeMethod={1|0}
;
; Имя или адрес сервера, с которым синхронизировать время рабочей станции
;
; @v6.0.8 Если после запятой установлено не нулевое значение,
; то при изменении системного времени в течении сеанса программа
; сверит новое время с сервером time_server_name и при расхождении
; вернет время на рабочей станции в значение, соответствующее серверу.
;
; Примеры:
; TimeServer=192.168.0.1
; TimeServer=CentralSever,1
;
TimeServer=time_server_name[,force]
;
; Электронный адрес службы поддержки
;
SupportMail=soupport_mail
;
; @v3.11.9 Загружать в кассовый модуль Кристалл-УКМ номера секций для товаров.
;
ExportCrukmSections={1|0}
;
; @v4.5.5  Данные из кассового модуля Петрович выгружать по штрихкодам
;
petrovi4barcode={1|0}
;
; @v4.9.0  Смещение идентификатора весового товара (default=200)
;
petrovi4bias=int_val
;
; @v4.8.0 Штрихкоды с минимальным приоритетом (в нижней части списка штрихкодов товара)
: Первое значение - префикс, второе - длина. Можно опустить как один так и второй параметр.
; Например: BarcodeLowPriority=,9 означает что штрихкоды с длиной 9 символов
; имеют самый низкий приоритет независимо от префикса.
;
BarcodeLowPriority=prefix,length
;
; @v4.8.3 В модуле управления фискальным регистратором
; Штрих-ФР не проверять вывод чека на продолжение печати.
; see SCS_SHTRIHFRF::AnnulateCheck() file SHTRIHFR.CPP
;
ShtrihFRNoUseContPrn={1|0}
;
; @v4.9.15 Параметры обмена для Штрих_ФР
; baud_rate=(2..6): 2-9600, 3-19200, 4-38400, 5-57600, 6-115200
; timeout=(0..255)
;
ShtrihFRConnectParam=baud_rate,timeout
;
; Не использовать весовые датчики наличия чековой ленты в Штрих-ФР
;
ShtrihFRNotUseWeightSensor=1
;
; tab_num_symb - набор символов в типе регистрационного документа,
;
CashierTabNumSymb=tab_num_symb
;
; @obsolete since v5.4.10
; Путь к каталогу, содержащему папки клиентов
;
PersonFolder=path_to_person_folder
;
; @v5.0.9 Описание формата для двумерных штрих-кодов
; min_barcode_len  - min длина штрих_кода, который надо преобразовать
; fmt_descr_offset - смещение описания формата [0..]
; fmt_descr        - описание формата (не более 15 символов)
; barcode_offset   - смещение штрих_кода [0..]
; barcode_len      - длина штрих-кода
; Например: TwosizeBarcodeFormat=20,,0178,12,6;20,0,0278,12,6
;
TwosizeBarcodeFormat=min_barcode_len,fmt_descr_offset,fmt_descr,barcode_offset,barcode_len[;...]
;
; @v5.1.9 Описание шаблонов персональных карт
; (если несколько - через ;, обязательно в кавычках)
; prefix, postfix - любой набор символов (включая ; и ", исключение - ";)
;
SCardPatterns="prefix*C*postfix"
;
; @v5.2.0 Описание операций для экспорта накладных в ЕГАИС
; op_id1 - для приходной накладной
; op_id2 - для расходной накладной
; op_id3 - для возвратной приходной накладной
; op_id4 - для возвратной расходной накладной
;
EGAISExportOpTypes=op_id1;op_id2;op_id3;op_id4
;
; Алгоритм формирования списка измененных товаров для асинхронных кассовых
; сессий.
; 0 - традиционный - перебирается весь справочник товаров для идентификации
;     тех позиций, цены по которым изменились. Список измененных товаров
;     извлекается из системного журнала
; 1 - традиционный с тестом алгоритма "по измененным документам". Переходный
;     режим, позволяющий оценить погрешность перехода на алгоритм 2. Позиции,
;     которые должны быть признаны измененными в соответсвии с алгоритмом 0,
;     но не признаны таковыми в соответствии с алгоритмом 2, заносятся в
;     файл журнала ppinfo.log сообщением
;     "AsyncCashGoodsIterator miss: [id=ид товара] наименование товара"
; 2 - алгоритм "по изменениям документов". По системному журналу извлекается //
;     список документов, которые были добавлены или изменены с момента
;     последней загрузки кассового узла. По документам, которые влияют на состояние
;     склада, извлекается список товаров, в него вливается список измененных
;     товаров, и перебор идет уже по результирующему списку, а не по всему
;     справочнику товаров
;
acgialg=[0,1,2]
;
; @v8.5.4
; Специальный параметр для идентификации нескольких различных цен на один
; товар. Задает количество дней, за которые необходимо просмотреть лоты
; товара на предмет наличия нескольких открытых лотов с различными ценами.
; Максимальное допустимое значение 730
; Отрицательные знания и значения, превышающие максимум трактуются как 0 (не определенные)
;
AcgiPriceLookBackPeriod=days
;
; @v5.3.12 Описание персоналии для автоматического заполнения пластиковых карт
; PersonName - имя персоналии без кавычек
;
SCardDefPerson=PersonName
;
; Максимальный размер файла с накладными для экспорта данных для поставщика
; Size - размер файла в килобайтах
;
SupplExpBillFileMaxSize=Size
;
; Имена DBF-файлов для экспорта в Сет-Ретейл дополнять временем создания //
;
SetR_AddTimeToFileNames=1
;
; @v5.5.9 При экспорте в Сет-Ретейл использовать новый алгоритм формирования кодов скидок
;
SetR_UseNewDscntCodeAlg=1
;
; Использовать ассоциацию товар-склад
; для печати чека на дополнительный кассовый узел (Штрих-ФР)
;
UseGoodsLocAssocToExtCashNode=1
;
; Заполнение таблицы продаж: размер кванта товарных идентификаторов
; по которому формируются записи в таблице. Чем больше это значение,
; тем быстрее осуществляется расчет, но, с другой стороны, при увеличении
; значения возникает вероятность того, что сервер базы данных не справится //
; с большими транзакциями (сообщение "Нет доступных кэш-буферов").
; Кроме того, при росте этой величины значительно увеличивается требование
; к доступной памяти на рабочей станции, на которой выполянется обработка.
; ! Указанное здесь значение округляется до степени 2 в меньшую сторону.
; По умолчанию: 512
; Максимальное допустимое значение: 32*1024
;
PSales_GoodsQuant=512
;
; Предельное время работы процесса заполнения таблицы продаж в секундах.
; Процесс построения после завершения каждого кванта товаров
; проверяет общее время работы и, если оно превышает указанное
; этим параметром, то процесс обрывается.
; Нулевое или отрицательное значение не ограничивает процесс по времени.
;
; Допустимый диапазон [0..24*60*60*7]
;
PSales_MaxTime=0
;
; Если значение параметра не ноль, то после каждого проведения //
; товарного документа система проверяет корректность лотов и текущих товарных
; остатков, затрагиваемых этим документом.
; Опция необходима из-за того, что с этой проблемой пора кончать.
;
debug_trfr_error=0
;
; @v7.3.11
; При ненулевом значении параметра в журнал mtxdirty.log будет
; выводиться информация об актуализации кэша товарной матрицы.
; Данный лог отладочный и нужен только на время идентификации
; проблем с означенной актуализацией.
;
debug_mtx_dirty=0
;
; @v8.1.9
; Начиная с версии 8.1.9 в 3-tier режиме клиентские сессии извлекают
; товарную матрицу с сервева. Если в механизме такого извлечения возникнут
; проблемы, то его следует отключить установкой данного параметра в 1
;
DontUse3TierGMtx=[0|1]
;
; @v5.8
; SubscribersList - список email-адресов, разделяемых символом ";",
; для рассылки уведомлений о выполнении задач сервером
;
; Пример:
; jobserver_notify=mail@petroglif.ru;admin@petroglif.ru
;
jobserver_notify=SubscribersList
;
; Принимать из другого раздела чеки с дублируемым ключем {Dt, Tm, CashID}
; Если параметр равен 1, тогда при приеме кассовых чеков
; из другого раздела программа будет смещать время чека, если
; в базе данных уже есть чек с такой же комбинацией {Dt, Tm, CashID}.
;
; ВНИМАНИЕ! Флаг аварийный. Предназначен только для временного применения.
; При неоправаднном использовании может привести к неправильному
; акцепту кассовых сессий.
;
AcceptDupTimeCheck=[0|1]
;
; @v7.5.9
; Если !0, то в диалоге товарного документа прихода (тип операции "Приход товара") при выборе
; адреса доставки выбор осуществляется из списка складов главной организации.
; Если параметр отсутствует или 0, то выбор осуществляется из списка адресов доставки
; контрагента (как для осталный типов операций, кроме драфт-прихода, для которого
; выбор склада как адреса доставки безусловный).
;
; Парамет проверяется только при запуске сеанса.
; @v9.1.11 Параметр устарел. Вместо него следует использовать соответствующий флаг в виде операции.
;
RcptDlvrLocAsWarehouse=[0|1]
;
; Символ склада слабоалкогольной продукции для экспорта данных в монолит CRM
;
BaltikaWeakAlcLocSymb=LocSymb
;
; Код группы слабоалкогольной продукции для экспорта данных в монолит CRM
;
BaltikaWeakAlcGGrpCode=GrpSymb
;
; Символ склада разливного пива для экспорта данных в монолит CRM
;
BaltikaWoTareBeerLocSymb=LocSymb
;
; Код группы разливного пива для экспорта данных в монолит CRM
;
BaltikaWoTareBeerGGrpCode=GrpSymb
;
; Код шлюза для слабого алкоголя
;
BaltikaWeakAlcCode = Code
;
;Код шлюза для разливного пива
;
BaltikaWoTareBeerCode = Code
;
;Код шлюза для сальдо по таре
;
BaltikaSaldoCode = Code
;
; Ид. дополнительного поля адреса доставки, в котором содержится
; адрес CRM для товаров horeca
;
BaltikaDlvrAddrExtFldId = Id
;
; Ид. дополнительного поля адреса доставки, в котором содержится канал поставки
;
BaltikaSaleChannelExtFldId = Id
;
; Символ склада бракованного товара для экспорта данных в монолит CRM
;
BaltikaSpoilageLocSymb=LocSymb
;
; Список контрагентов (статей), по которым не нужно считать сальдо
;
BaltikaExclSaldoContragents= ArId1,ArId2,ArId3 ...
;
; Класс товара алкоголь (используется в CrystallSetV10)
;
; proof(процент крепости) и volume(объем) принимают значения [x|y|w|z] из класса товара
GoodsClassAlc=GoodsClassSymb,proof,volume
;
; Использовать альтернативный вариант импорта кассовых сессий
; (для Сет-Ритейл или Атола, работающих со сторонними приложениями)
;
;
; Класс товара табак (используется в CrystallSetV10)
;
;
GoodsClassTobacco=GoodsClassSymb
;
;
; Класс товара подарочная карта (используется в CrystallSetV10)
;
;
GoodsClassGiftCard=GoodsClassSymb
;
; Использовать альтернативный вариант импорта кассовых сессий
; (для Сет-Ритейл или Атола, работающих со сторонними приложениями)
;
CloseAsyncSess_UseAltImport=1[;file_rpt,file_flag,file_goods,file_goods_flag]
;
;   file_rpt        - имя файла отчета по кассовым сессиям
;   file_flag       - имя файла запроса отчета по кассовым сессиям
;   file_goods      - имя файла справочника товаров
;   file_goods_flag - имя файла запроса справочника товаров
;
; file_XXX - необязательные параметры для импорта из Атола,
; при их отсутствии будут использоваться стандартные имена файлов из Папируса:
;   file_rpt        - atolexp.txt
;   file_flag       - atolexp.flg
;   file_goods      - goods.txt
;   file_goods_flag - getgoods.txt
;
; Адрес и порт (через :) Job сервера для получения информации о запущенных задачах
;
JobServerAddrPort=addr:port
;
; @v6.1.12
; Если значение следующего параметра != 0, то
; при формировании данных для отчетов система будет создавать
; файлы словарей. В противном случае, если используется словарь
; на основе DL600, то файлы словаря создаваться не будут.
;
; Ранее файлы словаря создавались в любом случае. Однако, как
; выясняется, для печати CrystalReports'у словарь не нужен,
; но требуется лишь в том случае, если по сформированным данным
; вы хотите создать или отредактировать отчет.
;
report_force_ddf=[1|0]
;
; @v7.9.9
; Идентификация существования файла базы данных осуществляется путем попытки открытия файла
; а не методом операционной системы, выявляющей существование файла.
; Опция экспериментальная - введенна в рамках борьбы с задержками при создании
; файлов Btrieve в Windows 7, Windows 8 и Windows Server 2008
; По умолчанию 1
; @v8.2.4
; Если установлено значение 100, то при подготовке данных для печати также используется функция Btrieve
;
DetectDbtExistByOpen=[1|0|100]
;
; @v8.0.2
; Параметры блокировки записей без ожидания.
; MaxTries - максимальное количество повторных попыток если запись заблокирована.
;   Если MaxTries=0 - то используются блокировки с ожиданием (как в версиях до 8.0.2)
; Delay - таймаут между повторными попытками блокировки в миллисекундах (по умолчанию 10).
; Если BtrNwLock отсутствует либо задан некорректно, то используются блокировки без ожидания с
; максимальным числом попыток 20 и таймаутом 10мс.
;
BtrNwLock=MaxTries[,Delay]
;
; @v7.0.10
;
; Минимальная средняя задержка между вводом последовательных
; символов в строке ввода, при значениях выше которой, система
; считает, что символы введены в ручную.
;
AutoInputTolerance=[1..100]
;
; @v7.0.10
; Строка команды, посылаемой на локальный принтер, оснащенный кухонным звонком,
; для подачи сигнала. Эта строка должна быть представлена набором пар шестнадцатиричных
; символов. Каждая пара преобразуется программой в бинарный байт, посылаемый на устройство.
; Например: 1B70000505.
; Если количество символов в строке нечетное либо хотя бы один из символов не
; является шестнадцатиричным [0-9A-Fa-f] строка считается не действительной и сигнал на принтер
; не подается (в файле pperror.log появится запись об ошибке).
; Если этот параметр пустой или отсутствует, то система подает команду по умолчанию.
;
KitchenBellCmd=hex_command
;
; @v10.0.12
; При работе с кассовым регистратором удерживать открытым соединение. 
; Опция введена в качестве возможности аварийного отключения режима (которые обещает увеличение 
; производительности при работе с оборудованием). 
; По умолчанию=1
;
PosRegisterKeepAlive=[1|0]
//
//
//
SpecEncodeSymbs=EncodeSymb1,SubStr1;EncodeSymb2,SubStr2...

	Где EncodeSymb1,EncodeSymb2... специальные символы которыми кодируются подстроки
	SubStr1,SubStr2...
;
; В таблицу похожих объектов заносятся только те объекты, рейтинг похожести которых
; >= LikenessRate. Если LikenessRate не задан или <=0, то считается что он равен 0.5
;
LikenessRate=0.5
;
; Символ базы данных для получения бизнес-показателей через TCP/IP (при этом
; используется учетная запись $SYSSERVICE$
;
BizScore_DbSymb=DbSymb
;
; В кассовой панели для сенсорного экрана элементы списка товаров и групп отображаются как кнопки
;
TSGGroupsAsButtons=[1|0]
//
// @v7.3.4
// В кассовой панели для сенсорного экрана список товарных групп не иерархический, но при выборе
// группы верхнего уровня следующий уровень полностью заполняет список. Возврат к следующему
// уровню осуществляется кнопкой [Back] (над списком).
//
TSGGroupListFlat=[1|0]
;
; Таймаут для Штрих-М-ФР-К на получение файлов с устройства
;
Shtrih_MFRK_Timeout=timeout (в милисекундах)
;
; @v4.5.10 Таймаут при загрузке чеков из кассового модуля АТОЛ (миллисекунды)
; По умолчанию 5000.
;
AtolTimeout=timeout
;
; При обновлении сессии в Атоле остатки за вычетом продаж
;
AtolQuantityWOSale=1
;
; Задержка между исчезновением сигнального файла и началом
; импорта из файла чеков, сформированного кассовым модулем Frontol
; Если значение параметра меньше нуля, то задержка не
; применяется вообще.
; Если параметр отсутствует или имеет нулевое значение, то
; применяется задержка по умолчанию (1000 миллисекунд)
;
FrontolImportDelay=timeout (в милисекундах)
;
; @v7.4.1
; Если этот параметр установлен в значении !0, то
; на кассовый модуль Фронтол кредитные карты
; загружаются так же как и дисконтные. В противном
; случае кредитные карты загружаются как сертификаты.
;
FrontolCrdCardAsDsc=[1|0]
;
; @v8.2.3
; Если значение параметра установлено в 1, то на frontol не будут загружаться дисконтные схемы
; По умолчанию 0
;
;FronotlSkipDiscountSchemes=[1|0]
FrontolSkipDiscountSchemes=[1|0]
;
; Возможность экспорта/импорта данных в excel
;
EnableExcelImpExp=[1|0]
;
; @v6.5.10 Заносить в журнал ccheck.log информацию об операциях с кассовымии чеками.
; До версии 6.5.10 признаком необходимости вести такой журнал был параметр debug=1.
; Так как необходимость ведения данного журнала определяется функциональными требованиями
; к использованию системы, пришлось ввести отдельный параметр для этого.
;
LogCCheck=[1|0]
;
; @v7.8.0 Строка символов, которые должны заменяться XML-сущностями при экспорте в XML
; средствами модуля DL600. Если первый символ '-', то последующие символы исключаются из замены.
; Например:
;   &<>'     будут заменены только приведенные символы
;   -,./\    будут заменены все, предполагаемые (программой) символы, кроме ,./\
;
DL600XMLEntity=
;
; @v7.9.6
; При преобразовании строки чека из кассовой панели в запись CCheckLine корректировать
; разницу за счет скидки.
; Необходимость в установке параметра может возникнуть при работе с товарами, которые продаются большими
; количествами и по ценам, имеющим более двух знаков после точки.
; Возможно, правило такой корректировки должно быть применено во всех случаях, однако, во
; избежании проблем с работающими инсталляциями, страхуемся вводом данного параметра.
;
adjcpancclinetrans=[1|0]
CodePrefixedList=[1|0]
;
; Если !0, то асинхронная очередь системных событий не применяется.
; Асинхронная очередь системных событий - это специальный поток, которых
; извлекает события из системного журнала и других источников для онлайнового оповещения
; пользовательского сеанса.
;
DisableAsyncAdvQueue=[1|0]
;
; @v8.6.0
; Период опроса потоком асинхронной очереди системных событий системного журнала (в миллисекундах).
;
AdviseEventCollectorPeriod=ms
;
; @v8.6.0
; Дата, начиная с которой применяется алгоритм 2 для расчета налогов по объединенным строкам счет-фактуры (для печати).
; Алгоритм 2 заключается в расчете налогов раздельно по каждой из объединяемых строк и последующем суммировании
; результата. Алгоритм 1, применяющийся по умолчанию (и ранее релиза 8.6.0) рассчитывает налоги по объединенной строке
; в целом.
; Отсутствие параметра или не допустимое значение даты трактуется как предписание применять традиционный алгоритм (1).
;
InvcMergeTaxCalcAlg2Since=date
;
; @v10.4.10
; Параметр, управляющий формированием отладочных журналов модуля gSOAP
; Вместо options должно быть либо all, либо перечесление одной или нескольких опций sent, recv, test
; all - будут выводиться 3 журнала (gsoap_sent.log, gsoap_recv.log, gsoap_test.log)
; sent - будет выводится журнал gsoap_sent.log
; recv - будет выводится журнал gsoap_recv.log
; test - будет выводится журнал gsoap_test.log
; Example:
;    gsoap_log=sent recv 
;    gsoap_log=all
;
gsoap_log=options
;
; ------------------------------------------------------------------------------
; Параметры для формирования кассового узла Papyrus
;
[pos_host]
;
name=наименование кассового узла
path=каталог для импорта/экспорта данных
;
; Параметры конфигурации (экспериментального) проекта Sartre
;
; Размер кэш-буфера базы данных Sartre. Транслируется в вызов функции BDB set_cachesize. default=1024M
sartredb_cachesize=data_size
; То же, что и sartredb_cachesize но для режима read-only. default=128M
sartredb_cachesize_ro=data_size
; default=512K
sartredb_maxlockers=data_size
; default=64K
sartredb_maxlockers_ro=data_size
; default=512K
sartredb_maxlocks=data_size
; default=32K
sartredb_maxlocks_ro=data_size
; default=512K
sartredb_maxlockobjs=data_size
; default=32K
sartredb_maxlockobjs_ro=data_size
; default=8M
sartredb_logbufsize=data_size
//
//
//
АЛГОРИТМ ВЫБОРА СИСТЕМОЙ ПУТИ К БАЗЕ ДАННЫХ

	1. Если секция [dbname] отсутствует, пустая, либо содержит только
	один путь, то используется путь заданный в секции [path] параметром
	dat.
	2. Если секция [dbname] содержит более одного пути, то во вводном
	диалоге при входе в программу предлагается выбрать один из этих
	путей (список содержит дружественные названия баз данных,
	перечисленные в значениях параметров секции до запятой).
	При выборе одного из названий, база данных ищется в каталоге,
	заданном подстрокой выбранного параметра после запятой.
	Если пользователь игнорировал выбор именованной базы данных, то
	действует п.1.
	3. Если при входе в программу командная строка содержала параметр
	/DB:name, то автоматом выбирается путь, соответствующий параметру
	name секции [dbname]. Отсутствие этого параметра в секции
	воспринимается как фатальная ошибка.
//
//
//
ФОРМАТ ОПИСАНИЯ ЭКСПОРТА ДАННЫХ В СИСТЕМУ КЛИЕНТ-БАНК

[client-bank]
	;
	; Путь и имя файла экспорта.
	; По умолчанию path\bnkorder.ext где path - каталог исходящих данных
	;     по настройке в базе данных, ext - расширение, соответствующее
	;     формату.
	;
filename=h:\ppy\out
	;
	; Кодовая страница, в которой должны быть представлены
	; текстовые поля. Может быть либо windows-1251 либо dos-866.
	; По умолчанию dos-866
	; *** Не используется ***
	;
codepage=windows-1251 | dos-866
	;
	; Дескриптор формата:
	; dbf - dbf-файл
	; csv - comma separated values - данные, разделенные запятыми
	; txt(c)  - текстовый файл. Одна строка - одна запись. Пол
	;           разделяются указанным в скобках разделителем.
	;           Если разделитель не указан, то полагается '|'.
	; По умолчанию dbf
	;
format=dbf | csv | txt(^)
	;
	; Опциональное поле, позволяющее идентифицировать принадлежность
	; платежного поручения банку, в который отправляется документ
	; (во избежании путаницы)
	; *** Не используется ***
	;
bankcode=bic
	;
	; ---------------------------------------------------------------------
	; Формат представления полей
	;
	; PapyrusFieldName=OutputFieldName,Type,Lenght,Decimals
	;
	; PapyrusFieldName - Наименование поле в системе Papyrus. См список ниже
	; OutputFieldName  - Наименование поля в выходном файле. Если значение опущено,
	;    то для структурированных (dbf) файлов полагается наименование по умолчанию,
	;    для неструктурированных вообще нет смысла ничего указывать.
	; Type             - Тип поля. Для dbf-файлов -
	;    N - числовое поле, D - дата, C - строка.
	;    Если опущено, то используется тип по умолчанию.
	; Length   - Максимальная длина поля. Если опущено, то используетс
	;            значение по умолчанию.
	; Decimals - Количество десятичных знаков после точки. Если опущено,
	;            то используется значение по умолчанию.
	; ---------------------------------------------------------------------
	; Наименования полей в системе Papyrus, соответствующие им параметры по
	; умолчанию
	;
Empty              ; Поле, не заполняемое системой, но требуемое модулем клиент-банк
	; Можно задать значение поумолчанию в скобках после текста Empty
	; Например: Empty(01)
BillID           = BILLID,   N,  10, 0  ; ИД платежного документа
Date             = DATE,     D,   8, 0  ; Дата платежного документа
Code             = CODE,     C,  10, 0  ; Номер платежного документа
Sequence         = SEQUENCE, N,   4, 0  ; Очередность платежа
Purpose          = PURPOSE,  C, 160, 0  ; Назначение платежа
PurposePlusVat   = PURPOSE,  C, 160, 0  ; Назначение платежа с текстовым представлением НДС
Amount           = AMOUNT,   N,  12, 2  ; Сумма платежа
PayerID          = PAYERID,  N,  10, 0  ; ИД персоналии плательщика
PayerName        = PNAME,    C, 128, 0  ; Наименование плательщика
PayerINN         = PINN,     C,  16, 0  ; ИНН плательщика
PayerBankAcc     = PBNKACC,  C,  24, 0  ; Банковский счет плательщика
PayerBankName    = PBNKNAME, C, 128, 0  ; Наименование банка плательщика
PayerBankCode    = PBNKCODE, C,  16, 0  ; БИК банка плательщика
PayerBankCorr    = PBNKCORR, C,  24, 0  ; Корр счет банка плательщика *
ReceiverID       = RCVRID,   N,  10, 0  ; ИД персоналии получател
ReceiverName     = RNAME,    C, 128, 0  ; Наименование получател
ReceiverINN      = RINN,     C,  16, 0  ; ИНН получател
ReceiverBankAcc  = RBNKACC,  C,  24, 0  ; Корр счет банка получател
ReceiverBankName = RBNKNAME, C, 128, 0  ; Наименование банка получател
ReceiverBankCode = RBNKCODE, C,  16, 0  ; БИК банка получател
ReceiverBankCorr = RBNKCORR, C,  24, 0  ; Корр счет банка получателя *
TaxPayerKPP      = PKPP,     C,  12, 0  ; КПП плательщика бюджетного платежа
TaxReceiverKPP   = RKPP,     C,  12, 0  ; КПП получателя бюджетного платежа
TaxPayerStatus   = PSTATUS,  C,   4, 0  ; Статус плательщика бюджетного платежа
TaxClass         = TAXCLASS, C,  12, 0  ; Код бюджетной классификации
OKATO            = OKATO,    C,  16, 0  ; Код по ОКАТО бюджетной организации
TaxReason        = REASON,   C,   8, 0  ; Код причина бюджетного платежа
TaxPeriod        = TAXPERIOD,C,  12, 0  ; Налоговый период
TaxDocNumber     = DOCNUMBER,C,  12, 0  ; Номер документа, согласно которому идет уплата
TaxDocDate       = DOCDATE,  D,   8, 0  ; Дата документа, согласно которому идет уплата
TaxPaymType      = PAYMTYPE, C,   8, 0  ; Тип бюджетного платежа
VatSum           = VATSUM,   N,  10, 2  ; Сумма НДС
//
//
//
ПАРАМЕТРЫ ДЛЯ РАБОТЫ С PRICE CHECKER SHUTTLE SG15/20

[ShuttleSG1520]
;
; Номер порта для ip-соединения
;
Port=value
;
; Срыть консольное окно при работе приложения. Значения: 0 - оставить окно, 1 - скрыть
;
HideWindow=0 | 1
;
; Точка входа в базу данных
;
DbSymb=name
;
; Имя пользователя, под которым будет осуществлен вход в базу данных
;
UserName=name
;
; Пароль для входа в базу данных
;
Password=pass
;
; Список складов, в которых приложение будет искать товар
;
Storage=store1;store2
;
; Формат выводимых данных на дисплей устройства SG15.
;
sg15string=string=<ESC>B0<ESC>$<ESC>.0{name}<ETX><#0x0A>{info}<ESC>B1<ESC>.8{price} <#0xF0>.<ETX><ESC>B0
;
; Формат выводимых данных на дисплей устройства SG20.
;
sg20string=<ESC>%<ESC>'01{name}<ESC>'03{info}<ESC>'06           <ESC>=2000<#128> {price}<ESC>=1000
