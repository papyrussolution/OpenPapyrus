Тест TDDO2
Первый текстовый блок!

#set($x = 10 + $y * (3 - 1.2))

Еще текст какой-то.

#if($cls.abc < $cls2.foo($x, "гыгы")) будет так. 
#elseif($cls3.boo() >= 5) или вот-так!
#else будет иначе! #endif

#macro(abc $a $b $c)
	<$a>  a </$a>
	<$b> b ${c} </${b}>     	
#end

#abc("первый параметр", "второй параметр", "третий параметр")

Last text block!
